fragment UserFragment on User {
    id
    createdAt
    updatedAt
    name
    username
    avatarUrl
}

query User($username: String!) {
    user(username: $username) {
        ...UserFragment
    }
}
